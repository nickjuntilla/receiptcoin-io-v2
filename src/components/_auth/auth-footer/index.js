// Library imports
import React from 'react';

// Relative Imports
import { Container } from './styles';

const AuthFooter = (props, children) => {
  return <Container>{props.children}</Container>;
};

export default AuthFooter;
